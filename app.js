let attendance=[true, true, false, true, false];

function showatattendance(){
    let present=0;
    let absent=0;

    let list=
    document.getElementById("attendance-list");
    list.innerHTML="";

    attendance.forEach(function(status, index) {
        let il  
    }    

           
    }